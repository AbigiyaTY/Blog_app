<div id="singup" class= "container userBG py-3">
<h2 class="welcome text-center">Hello! you can register here.</h2>
<div class="sign-up-container">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field text-center my-3">
    <%= f.label :name, class: "flabel" %>:
    <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "finput" %>
  </div>
  <div class="field text-center my-3">
    <%= f.label :email, class: "flabel" %>:
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "finput" %>
  </div>
 <div class="field text-center my-3">
    <%= f.label :bio, class: "flabel bioFlabel" %>:
    <%= f.text_field :bio, autofocus: true, autocomplete: "name", class: "finput" %>
  </div>
  <div class="field text-center my-3">
    <%= f.label :photo_url, class: "flabel" %>:
    <%= f.text_field :photo, autofocus: true, autocomplete: "name", class: "finput" %>
  </div>
  <div class="field pwNew my-3">
    <%= f.label :password, class: "flabel" %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %>:
    <%= f.password_field :password, autocomplete: "new-password", class: "finput" %>
  </div>
  <div class="field newPW my-3">
    <%= f.label :password_confirmation, class: "flabel" %>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "finput" %>
  </div>
  <div class="actions text-center submit">
    <%= f.submit "Sign up", data: { turbo: false}, class: "sign-up-btn p-2" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
</div>
</div>














